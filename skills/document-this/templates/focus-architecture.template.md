> ⚠️ This document is auto-generated by `/document-this --focus "{{FEATURE_NAME}}"`.
> Edit the codebase, not this file.
> Last generated: {{DATE}}
> Project: {{PROJECT_NAME}}

# {{FEATURE_NAME}} — Architecture

Developer-facing orientation for the **{{FEATURE_NAME}}** subsystem. Reading this should be enough to find your way around in under 10 minutes. For the user-facing workflows, see [workflows.md](./workflows.md).

> For project-wide architecture, see the top-level [architecture.md](../architecture.md).

---

## Stack

Only the libraries and runtimes that this subsystem actually exercises.

{{STACK_ITEMS}}

---

## Directory map

The subsystem is spread across these locations. Each row says what that path contributes to **this feature** specifically — not what it contains in general.

{{DIRECTORY_MAP_TABLE}}

---

## Data model

Persisted tables and in-memory entities that the feature owns. Field-level detail only when the JSON shape matters — otherwise just one line on purpose and access controls.

{{DATA_MODEL_SECTIONS}}

<!--
For each entity:
  ### `<table_or_class_name>` (one-line purpose)
  - **Migration / source:** ../path/with/back-link
  - **Columns / fields:** key columns and what they carry (esp. JSON shapes)
  - **Model:** ../path/to/model
  - **Purpose:** one or two sentences
  - **Access controls:** policy / route guards / scopes
  - **Exposed via:** web UI / API endpoint / job
-->

---

{{#if PIPELINE_SECTION}}
## {{PIPELINE_TITLE}}

{{PIPELINE_DIAGRAM}}

{{PIPELINE_NARRATIVE}}

<!--
The pipeline section is high-value for features that have a notable end-to-end flow:
ingest pipelines, request lifecycles, build chains, etc. Use an inline ASCII or Mermaid
diagram and 1–3 paragraphs of narrative. Omit this section entirely if the feature is
mostly CRUD.
-->

---
{{/if}}

## Patterns & conventions

Conventions specific to this subsystem — not the project as a whole. Each bullet should be one or two sentences with the rule first, then the rationale or a pointer.

{{PATTERNS_AND_CONVENTIONS}}

---

{{#if MAPPING_SECTION}}
## {{MAPPING_TITLE}}

{{MAPPING_TABLE}}

<!--
Optional mapping section: when the user's mental model differs from the implementation,
include a table that translates "what the user expects" → "what actually exists" → "where
to find it". Useful for features the user has asked questions about, or for legacy
subsystems where naming has drifted.
-->

---
{{/if}}

## Notable files at a glance

5–15 files a developer working on this subsystem will keep returning to. Use `../`-prefixed links.

{{NOTABLE_FILES_LIST}}

---

## Diagrams

- **[diagrams/{{SLUG}}-data-model.mmd](./diagrams/{{SLUG}}-data-model.mmd)** — entities and relationships.
- **[diagrams/{{SLUG}}-architecture.mmd](./diagrams/{{SLUG}}-architecture.mmd)** — end-to-end flow.
