> ⚠️ This document is auto-generated by `/document-this --focus "{{FEATURE_NAME}}"`.
> Edit the codebase, not this file.
> Last generated: {{DATE}}
> Project: {{PROJECT_NAME}}

# {{FEATURE_NAME}} — Workflows

This file describes how people interact with the **{{FEATURE_NAME}}** subsystem in {{PROJECT_NAME}}. Each workflow is backed by a test or by another concrete code path. Plain-English language is used throughout — see [architecture.md](./architecture.md) for code-level detail.

> For workflows outside this subsystem, see the top-level [workflows.md](../workflows.md).

---

## Who interacts with {{FEATURE_NOUN}}

{{AUDIENCE_OVERVIEW}}

<!--
Audience overview is a short numbered list (2–5 bullets) of the distinct user roles or
systems that touch this feature, with a one-sentence description of how each uses it.
Examples of role labels: "Org admins", "End designers", "Developers (build time)",
"Partner systems (S3 drops)", "Super admins". This sets up the workflow grouping below.
-->

---

{{#if NO_FEATURE_TESTS}}
> ⚠️ **No system or integration tests were found for {{FEATURE_NAME}}.** Workflows below are inferred from source code and may be incomplete or inaccurate.
{{/if}}

{{WORKFLOW_SECTIONS}}

<!--
Each workflow section uses templates/workflow-entry.template.md. Include:
- A "What it does NOT do" subsection when there are common misconceptions to head off.
- A "Where this is tested" line citing the spec file(s) with ../ path prefix.
- A `mermaid sequenceDiagram` for the 1–3 most central workflows.

Group by audience (use the audience labels from the overview above as section headings),
or by lifecycle stage if that maps the feature more naturally.

Number workflows sequentially across the file (Workflow 1, Workflow 2, ...) so they can
be cross-referenced inline.
-->
