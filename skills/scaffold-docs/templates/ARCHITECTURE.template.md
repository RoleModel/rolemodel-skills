# Architecture

## Tech Stack

<Language, framework, database, frontend, auth, jobs, testing — one bullet each,
naming the choice only. No version numbers: name the manifest that pins them
(`.ruby-version`, `Gemfile`, `package.json`) so the reader checks the file that
can't be out of date.>

## Data Model & Domain

<The few facts that shape every model: the tenancy rule, the authorization
mechanism, the core entities. Not an entity list.>

## Key Directories

<Only the ones whose purpose the framework's conventions don't already give away.>

## Notable Patterns

<Structural decisions a newcomer would mistake for accidents. Leave empty until
one exists.>
