# <Project Name>

<One sentence: what this application is, plus the one fact that changes how every
task in it is approached — e.g. "Multi-tenant: scope data and behavior to the
current workspace.">

## Before Editing

- Read `docs/CONVENTIONS.md`, then the convention files relevant to the task.
- Prefer an existing local pattern to a new one.
- Keep changes minimal and focused; preserve unrelated work in a dirty tree.
- Write a focused failing test before implementing feature behavior.
- See `docs/INDEX.md` and `docs/ARCHITECTURE.md` for structure and deeper docs.

## Verification

- <test command>
- <lint command>
